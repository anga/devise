require 'test_helper'

class SecurePasswordTest < ActiveSupport::TestCase
  test 'secure_password should not accept consecutive characters' do
    omit("Todo")
  end

  test 'secure_password should require upper and lower characters' do
    omit("Todo")
  end

  test 'secure_password should require symbols' do
    omit("Todo")
  end
end
